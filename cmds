BOT-2DG + FRONT CHATBOT NOTIFICAÇÃO AUTOMÁTICA

novo 23

sudo su root

cd-

apt update 
apt upgrade.

curl -fs8L http://deb.nodesource.com/setup 14.x sudo - bash -

sudo apt-get install -y nodejs

apt-get install libnss3-dev libgdk-pixbuf2.0-dev libgtk-3-dev libxss-dev libasound2

git clone https://github.com/pedroherpeto/zapdangalaxias.git

cd zapdasgalaxias

sudo npm install

npm start

sudo npm install -g pm2 

pm2 start botzdg.js